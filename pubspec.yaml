name: material_color_gen
description: A Flutter package to generate a MaterialColor from a Color
version: 1.0.0
homepage: liamedwardsnz.com

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

# The following section is specific to Flutter.
flutter:
